<div class="container">
<!--Fixed tab example -->
<div class="card pmd-card">
  <div class="pmd-tabs pmd-tabs-dark bg-primary" scroll="true">
      <ul role="tablist" class="nav nav-tabs nav-fill">
        <li class="nav-item" role="presentation"><a class="nav-link active" data-toggle="tab" role="tab" aria-controls="home" href="#home-fixed">Default</a></li>
        <li class="nav-item"><a class="nav-link" data-toggle="tab" role="tab" aria-controls="profile" href="#about-fixed">Fixed</a></li>
        <li class="nav-item"><a class="nav-link" data-toggle="tab" role="tab" aria-controls="messages" href="#work-fixed">Scrollable</a></li>
      </ul>
  </div>
  <div class="card-body">
      <div class="tab-content">
        <div role="tabpanel" class="tab-pane active" id="home-fixed">A tab provides the affordance for displaying grouped content. A tab label succinctly describes the tab’s associated grouping of content.</div>
        <div role="tabpanel" class="tab-pane" id="about-fixed">Fixed tabs have equal width, calculated either as the view width divided by the number of tabs, or based on the widest tab label. To navigate between fixed tabs, touch the tab or swipe the content area left or right.</div>
        <div role="tabpanel" class="tab-pane" id="work-fixed">
           <% current_user.following.each do |followed| %>
                    <% followed.posts.each do |post| %>
                  
               <div class="d-flex flex-column border rounded p-2 mb-2">
               <%= link_to post_user_path(post, post.user), :action => 'go' do %>
                <%= image_tag user_avatar(post.user), class:"border rounded-circle img-profile"%>
                <%= post.user.username.capitalize %>
               <%end%>
                
                <hr> 
                <div class="post-content">
                      <%= simple_format(post.content) %>
                  </div>
                  <hr>
                  <div class="d-flex flex-row">
                    <div>
                        <%= link_to post_path(post), class:"border p-2 shadow-sm rounded", :action => "go" do %>
                          <i class="fas fa-comments"></i> Read or Comment
                        <%end%>
                    </div>
                    <div>
                      <%= link_to like_post_path(post),class:"like-btn", method: :put, remote: :true do%>
                                <span><i class="fas fa-thumbs-up"></i></span>
                                
                        <% end %>
                        <span class="likes-count"><%= post.get_upvotes.size %></span>
                    </div>
                  </div>  
              </div>
                <% end %>
                <% end %>
        </div>
      </div>
  </div>
</div> <!--Fixed tab example end-->
</div>
